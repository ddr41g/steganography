import cv2
import os
import string

img=cv2.imread("spidey.jpg")
msg=input("Enter your secret phrase:")
pass=input("Enter a passkey:")

d={}
c={}

for i in range(255):
    d[chr(i)]=i
    c[i]=chr(i)

n=0
m=0
z=0

for i in range(len(msg)):
    img
